# Data directory

This directory will store JSON files for persistence in future iterations.

Examples of likely files:
- students.json
- classes.json
- assessments.json
- notifications.json

Current iteration also persists consolidated daily notifications in:
- notifications.json (one record per student/day with consolidated changes)